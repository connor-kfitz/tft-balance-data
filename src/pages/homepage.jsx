import React from "react";
import "../styles/homepage.css"

export default function Homepage() {

    return (
        <section>
            <div id="homepageCont">
                <p>Welcome to the TFT data collection app!</p>
                <p>Select one of the tabs above to sort items and traits, based on current balance state.  Submit your results, then navigate to the statistics page to view the overall results.</p>
            </div>
        </section>
    );
}